연산자 함수.
-연산자를 이용하듯 함수를 호출하여 연산을 수행할 수 있다.
-논리연산자들을 다중 정의로 사용해서는	안된다.

산술연산자
- int 같은 기본형 타입에 대해 +, -, *, /, % 연산자를 다중 정의할 수 있다.
-하지만 클래스로 만든 인스턴스와 같은 연산, 예로 CMytest 클래스의 인스턴스인 data + 34; 같은 연산을 실행하려면	 CMytest 클래스 내부에 operator+ 함수를 정의해야 한다.

